/**
 * Contains all the global variables needed for the project
 * @type {{domain: string, backend: string}}
 */
const global = {
    domain: 'http://localhost:8080',
    backend: 'http://localhost:3000'
}

export default global;